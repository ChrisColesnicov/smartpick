const AbstractRepository = require("./AbstractRepository");

class ConnectivityRepository extends AbstractRepository {
  constructor() {
    super({ table: "connectivity" });
  }

  async read(id) {
    const [rows] = await this.database.query(
      `select * from ${this.table} where id = ?`,
      [id]
    );
    return rows[0];
  }

  async readAll() {
    const [rows] = await this.database.query(`select * from ${this.table}`);
    return rows;
  }
}

module.exports = ConnectivityRepository;
